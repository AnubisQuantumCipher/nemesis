# NEMESIS Desktop — Defensive Hardening Re-Review Brief (2026-08-22, trust-surface epoch)

This is a **defensive code audit** for the application's own maintainers, re-run
on the trust-surface integration bytes. The goal is to confirm that the security
invariants the project documents are actually enforced in source, and to flag any
place where a defensive check is missing or weaker than its stated contract. Do
not produce attack tooling; report findings as maintainer-facing hardening
guidance.

Subject is frozen. Audit exactly this state:

- Repository: `/Users/sicarii/Worktrees/nemesis-desktop-production-recovered`
- Subject commit: `9dd22f259d164ebb1afde3b8940a891161cad3f6`
- Inputs manifest: `receipts/production-readiness-20260821/security-review/INPUTS-20260822.json`

## Ground rules (mandatory)

1. READ-ONLY. Do not edit, build, install, spawn subagents, or run package managers. Static reading and `git log`/`git diff`/`git show` read commands only.
2. Defensive posture only. For each invariant, read the cited code and decide CONFIRMED (the check exists and is sufficient, cite `path:line`) or GAP (describe the missing/weaker validation in maintainer terms and the exact defensive fix). No exploit code, no attack walkthroughs.
3. You receive no lead reasoning. Base every statement on file bytes at the subject state; cite `path:line`.
4. Product scope: local macOS desktop application only — Tauri app in `desktop/`, Ada daemon in `daemon/`, SPARK kernel in `kernel/`, Rust runtime in `runtime/`, protocols in `protocols/`, gates in `scripts/` and `tests/`. Excluded: iOS/Android/web/Windows/Linux/SaaS.
5. CHANGE OF EPOCH: the previously architect-gated residuals TS-001 and TS-002 were IMPLEMENTED on 2026-08-22 under the exact architect contract archived at `receipts/production-readiness-20260821/trust-surface/TS_CONTINUATION_CONTRACT_2026-08-22.md` (sha256 `920900f3a7d37a9a3e1d51541997070789a0e1e7bddecf31808076c67a00d3f9`). The implementation IS in scope: `authorize_action` now loads a persisted parent grant, derives an attenuated child through the SPARK-proved `Derive_Child_Grant`, and consumes a persisted one-shot approval durably before the authorization event. Audit it as shipped code, not as a residual.
6. Context docs: `THREAT_MODEL.md`, `TRUST_BOUNDARIES.md`, `SECURITY.md`, `docs/architecture/FORMAL_ASSURANCE.md`, `protocols/local-core-v1.md`, `docs/release/SOURCE_INSTALL.md`, scope `config/formal-kernel-scope.json`.

## Required output — one JSON object, nothing else

Identical schema to the 2026-08-21 brief (`nemesis.hardening-audit.result/v1`):
schema, lane, reviewer_identity{context,model,limitations[]}, subject_commit,
coverage[{invariant,files,status:CONFIRMED|GAP}], gaps[{id,severity,category,
cwe,path,line,precondition,impact,evidence,remediation,confidence}],
confirmed_invariants[], verdict CLEAN|GAPS|CRITICAL.

Verdict: `CLEAN` = every checked invariant CONFIRMED, no gap ≥ medium; `GAPS` = at least one medium/high gap; `CRITICAL` = a documented invariant is not enforced at all. Do not inflate severity; do not omit a real gap.
