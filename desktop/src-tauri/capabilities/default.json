{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "NEMESIS Desktop renderer receives only Tauri core defaults and named application commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
